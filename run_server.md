# Alpha Algo AI 실행 가이드 🚀

프로젝트를 실행하고 테스트하는 모든 명령어 모음입니다.

### 0. 가상환경 설정 (Virtual Environment)
먼저 가상환경을 활성화해야 합니다. 터미널을 새로 열 때마다 실행해주세요.

```bash
source .venv/bin/activate
```
*(터미널 프롬프트 앞에 `(.venv)`가 표시되면 성공입니다)*

---

### 1. 서버 시작 (Start Server)
웹 서버를 실행하여 서비스를 시작합니다.
*(가상환경이 활성화된 상태에서 실행하세요)*

```bash
PYTHONPATH=. uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```
`http://localhost:8000` 접속 가능

---

### 2. 주문/거래 테스트 (Test Orders)
주식 구매, 판매(지정가, 시장가, 시간외 등) 기능이 정상 작동하는지 확인하는 스크립트입니다.
*(서버 켜져 있는지 여부와 무관하게 독립적으로 실행 가능)*

```bash
export PYTHONPATH=$(pwd)
python scripts/manual_tests/test_orders_comprehensive.py
```
**테스트 항목:**
- 지정가/시장가 구매 및 판매
- 시간외 단일가 구매 및 판매
- 장전/장후 시간외 (모의투자에선 실패가 정상)

---

### 3. 서버 중지 (Stop Server)
`Address already in use` 메시지가 나오거나 서버를 강제로 종료할 때 사용합니다.

```bash
lsof -ti:8000 | xargs kill -9
```

---

### 4. 문제 해결 (Troubleshooting)
- **포트 충돌**: 위 3번(서버 중지) 명령어를 실행한 후 다시 1번을 실행하세요.
- **의존성 오류**: `uv sync` 명령어를 입력하여 라이브러리를 업데이트하세요.
- **ModuleNotFoundError**: 가상환경이 활성화되었는지 확인(`source .venv/bin/activate`)하거나 `PYTHONPATH=.`이 명령어 앞에 있는지 확인하세요.
